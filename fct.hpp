#ifndef FCT_H
#define FCT_H
#include <SDL2/SDL.h>
#include <iostream>
#include <string>

class Game{
  public:
    Game(std::string Title);
    class Window{
      public:
        Window(int width, int height);
        class Background{
          public:
          private:
        };
        class Character{
          public:
          private:
        };
      private:
        void Makewindows(){}
    };
  };
#endif
